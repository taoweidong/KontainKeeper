# KontainKeeper ACL 文件（mosquitto.conf 引用）
#
# 用户名约定：
#   - kk-server  ：服务端桥接账号（固定），需要全主题读写（订阅 status/hb/result、发布 cmd）。
#   - <主机名>   ：每台 Agent 一个用户，用户名即其在管理平台上的主机标识。
#
# pattern 行的 %u 由 Mosquitto 按「认证用户名」展开：
#   用户 web-01 登录后，只能读写 kk/v1/web-01/# 这一棵子树，看不到别的主机。

# 服务端桥接账号：全主题读写（普通订阅即可；多实例扩容再补 $share 组权限）
user kk-server
topic readwrite kk/v1/#

# Agent：每主机一个用户，仅允许读写自身主题子树
pattern readwrite kk/v1/%u/#
