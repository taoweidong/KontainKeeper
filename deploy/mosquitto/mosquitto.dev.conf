# KontainKeeper 本地开发 / 冒烟用 Mosquitto 配置
#
# 开匿名、无 ACL 文件，免去生成密码文件的步骤，docker compose up 即可一键起栈。
# 仅用于本机联调与 CI 集成测试；生产环境请改用 mosquitto.conf + gen-credentials.sh。

listener 1883

allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest stdout
