# Mosquitto 密码文件示例（真实文件 passwordfile 已加入 .gitignore，禁止入库）
#
# 生成方式（二选一）：
#   docker compose run --rm mosquitto mosquitto_passwd -b deploy/mosquitto/passwordfile kk-server <强密码>
#   bash deploy/mosquitto/gen-credentials.sh <主机名> <token>     # 逐主机账号
#
# 格式：<用户名>:<PBKDF2-SHA512 哈希>（Mosquitto 2.x 的 $7$ 格式，310000 次迭代）
# 用户名语义（见 aclfile）：kk-server 为服务端桥接账号；其余账号的用户名 = Agent 主机名，
# 对应 ACL 的 pattern readwrite kk/v1/%u/# —— 一台主机只能读写自己的主题。

# 下面是示例账号，**口令就是示例串本身**，切勿在生产使用：
kk-server:$7$310000$REPLACE_WITH_YOUR_OWN_HASH$REPLACE_WITH_YOUR_OWN_HASH
